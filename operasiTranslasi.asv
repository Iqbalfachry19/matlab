Tx = 20;
Ty = 50;
A = imread ('cameraman.tif');
[brs kol] = size(A);
for X = 1 : brs
for Y = 1 : kol
B(x+Tx, y+Ty) = A(x,y);
end
end
B = B(1:brs,1:KOL);
figure, imshow (uint8(A));
figure, imshow(uint8(B));
